# Selecciona todo de la tabla usuarios
SELECT * FROM usuarios;

#Selecciona campos de la tabla usuarios

SELECT email FROM usuarios;

#Operadores aritmeticos
SELECT id,email,(id + 7) AS 'OPERACION' FROM usuarios;
